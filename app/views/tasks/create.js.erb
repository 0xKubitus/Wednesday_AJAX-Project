<%# 
To double-check that AJAX is working -> 
alert("le fichier JS est exécuté!");
%>

document.getElementById("task-list-<%= @category.title %>").insertAdjacentHTML("beforeend", "<%= j render 'task', task: @task %>");
